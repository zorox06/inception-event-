# FarmState Configuration
PORT=3001

# Google Gemini API Key (Optional - the app runs fully autonomously with built-in heuristic core if omitted)
GEMINI_API_KEY=

# Gemini Model Version (default: gemini-2.0-flash)
GEMINI_MODEL=gemini-2.0-flash
